import * as React from "react";

const HiddenNavBar = () => {

    return (
        <header>
            <nav className="hidden">
                Login or Sign Up
            </nav>
        </header>
    )
}

export default HiddenNavBar;